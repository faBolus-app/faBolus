# garmin — Connect IQ (Monkey C) remote

Milestone 2: minimal units picker + confirm, messaging the iOS host via the Connect IQ mobile SDK. Validates against `../schema/command.schema.json`. Requires the Connect IQ SDK + a compatible Garmin watch.
